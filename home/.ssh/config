# Machine/private overlays (see dotfiles README). Includes first so per-machine
# values win (ssh uses the first-obtained value for each parameter).
Include ~/.ssh/config.local
Include ~/.ssh/config.local.host

# default
Host github.com
  HostName github.com
  User git
  IdentityFile ~/.ssh/id_ed25519
