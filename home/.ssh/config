# Private overlay (see dotfiles README). Included first so its values win
# (ssh uses the first-obtained value for each parameter). ssh's Include cannot
# expand $HOSTNAME, so per-host ssh tweaks also go in this file.
Include ~/.ssh/config.private

# default
Host github.com
  HostName github.com
  User git
  IdentityFile ~/.ssh/id_ed25519
